
Процедура ПередЗаписью(Отказ)
	ДляУдаления = Новый Массив;
	
	Для каждого Строка из УГМК_Партии цикл
		Если Строка.Партия = Номер и Строка.ДопНомер = ДопНомер тогда
			ДляУдаления.Добавить( Строка);
		КонецЕсли;
	КонецЦикла;
	
	Для каждого Строка из ДляУдаления цикл
		УГМК_Партии.Удалить( Строка);
	КонецЦикла;
КонецПроцедуры
