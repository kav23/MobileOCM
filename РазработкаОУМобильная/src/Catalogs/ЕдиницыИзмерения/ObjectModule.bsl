
// Обработчик события ПередЗаписью .
//
Процедура ПередЗаписью(Отказ)
	
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Отказ = Истина;
	
КонецПроцедуры // ПередЗаписью()

Процедура ПроверитьВес( СпрОбъект)
	Если СпрОбъект.ЕдиницаПоКлассификатору = Константы.ЕдиницаИзмеренияВеса.Получить() тогда
		СпрОбъект.Вес = СпрОбъект.Коэффициент;
	КонецЕсли;
КонецПроцедуры

